z4

redis	- db
jwt
uuid 	- sid 

--

r =  redis.Redis()
sid = 'foo' # sid = str(uuid.uuid4())
r.hset('bach':webapp:foo', 'login', 'chaberb') # r.hest ...
r.het('bach:webapp:' + sid : {'login')

-- db
login
sha(pass + salt)
salt